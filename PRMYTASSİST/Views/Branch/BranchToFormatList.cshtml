@using Entity
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    User CurrentUser = Session["CurrentUser"] as User;
    List<BranchFormat> branchFormats = Query.getBranchFormat();
    List<Branch> branches = Query.Branches();
}

<div class="kt-content  kt-grid__item kt-grid__item--fluid" id="kt_content" style="padding-top: 5%;">
    <form class="kt-form kt-form--label-right" method="post">
        <div class="kt-portlet kt-portlet--mobile">
            <div class="kt-portlet__head kt-portlet__head-label center pr-3">
                <div class="row pt-2 mt-2 pb-2 mb-2">
                    <div class="col-md-4" style=" align-self: center; ">
                        <select id="BranchFormatID1" class="custom-select form-control" style="color:red; font-weight:bold;" name="BranchFormatID">
                            <option value="0">FORMAT SECİNİZ - TÜMÜ</option>

                            @foreach (var item in branchFormats.Where(q => q.Visible == true))
                            {
                                <option value="@item.ID">@item.FormatName.ToUpper()</option>
                            }
                        </select>
                    </div>
                    <div class="col-md-8 mt-1" style=" text-align-last: right; ">
                        <a id="deneme" class="btn btn-brand btn-elevate btn-icon-sm">
                            <i class="flaticon2-plus"></i>
                            Şube Ekle
                        </a>
                    </div>
                </div>
            </div>
            <div class="kt-portlet__body pt-2">
                <!--begin: Datatable -->
                <table class="table table-striped- table-bordered table-hover table-checkable" id="kt_table_brachesFormat">
                    <thead>
                        <tr>
                            <th>Sıra</th>
                            <th>Şube Formatı </th>
                            <th>Şube Adı</th>
                            <th>Şube Kodu</th>
                            <th>Oluşturma Tarihi</th>
                            <th>İşlem</th>

                        </tr>
                    </thead>
                </table>
                <!--end: Datatable -->
            </div>
        </div>
    </form>
</div>
<!--begin:: Global Mandatory Vendors -->
<script src="./assets/vendors/general/jquery/dist/jquery.js" type="text/javascript"></script>
<script>
    $(document).ready(function () {
        //var id = $('#branchForUserSelect').val();
        getBranchesFormatList();
    });
</script>
<script>
    $("#deneme").click(function () {
        debugger
        ///Branch/BranchFormatDefinitions?id=BranchFormatID1.value
        var format = $('#BranchFormatID1').val();
        var url = '@Url.Action("BranchFormatDefinitions", "Branch")?id=' + format;
        window.location = url;
    });
</script>
